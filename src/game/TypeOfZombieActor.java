package game;

/**
 * This {@code enum} class defines the names of the different types of {@code Actor} that 
 * exists in the zombie game.
 */
public enum TypeOfZombieActor {
    ZOMBIE, HUMAN, PLAYER, FARMER, MAMBOMARIE, HEALER
}
